services:
  backend:
    build: ./backend
    ports:
      - "7081:7081"
    environment:
      - ASPNETCORE_URLS=http://+:7081
    networks:
      - app_network

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - app_network

networks:
  app_network:
    driver: bridge
